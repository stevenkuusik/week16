<!DOCTYPE html>
<html lang="et">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CV – <%= cv.steven %> <%= cv.kuusik %></title>
  <link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@400;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="cv-container">

    <div class="top">
      <div class="profile">
        <img src="/<%= cv.profiil %>" alt="Profiil">
        <div class="profile-info">
          <h2><%= cv.steven %> <%= cv.kuusik %></h2>
          <div class="contact-row"><%= cv.asukoht %></div>
          <div class="contact-row">
            <span>📞 <%= cv.kontakt.telefon %></span>
            <span>✉️ <%= cv.kontakt.email %></span>
          </div>
          <div class="contact-row">📍 <%= cv.kontakt.aadress %></div>
          <div class="stars">
            <% for (let i = 0; i < 5; i++) { %>
              <span class="filled">★</span>
            <% } %>
          </div>
        </div>
      </div>
      <div class="licenses">
        <h3>Juhiload</h3>
        <%= cv.juhiload.join(", ") %>
      </div>
    </div>

    <div class="content">
      <div class="left">
        <div class="section">
          <h3>Keeleoskus</h3>
          <% cv.keeled.forEach(k => { %>
            <p><%= k.nimi %> — <%= k.tase %></p>
          <% }) %>
        </div>

        <div class="section">
          <h3>Arvutioskused</h3>
          <% cv.arvutioskused.forEach(a => { %>
            <p><%= a %></p>
          <% }) %>
        </div>

        <div class="section">
          <h3>Huvialad</h3>
          <% cv.huvialad.forEach(h => { %>
            <p><%= h %></p>
          <% }) %>
        </div>

        <img id="carousel-img" class="chart-img" src="/<%= cv.karussell[0] %>" alt="Pilt">
        <div class="social"></div>
      </div>

      <div class="margin-right">
        <div class="section">
          <h3>Hariduskäik</h3>
          <% cv.haridus.forEach(h => { %>
            <p><strong><%= h.kool %></strong> — <%= h.aasta %></p>
          <% }) %>
        </div>

        <div class="section">
          <h3>Töökogemus</h3>
          <% cv.tootamine.forEach(t => { %>
            <p><strong><%= t.firma %></strong> — <%= t.amet %> (<%= t.aasta %>)<br>
            <%= t.kirjeldus %></p>
          <% }) %>
        </div>

        <div class="right">
          <div class="section">
            <h3>Täiendav info</h3>
            <p><b>Tugevad küljed:</b> <%= cv.tugevad.join(", ") %></p>
            <p><b>Nõrgad küljed:</b> <%= cv.norgad.join(", ") %></p>
            <p><b>Ajateenistus:</b> <%= cv.ajateenistus %></p>
            <p><b>Praktikale võimalik astuda:</b> <%= cv.praktika %></p>
          </div>
          <div class="social">
            <a href="https://instagram.com" target="_blank">
              <img src="instanupp.png" alt="Instagram">
            </a>
            <a href="https://facebook.com" target="_blank">
              <img src="fbikoon.png" alt="Facebook">
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script>
  window.carouselImages = <%- JSON.stringify(cv.karussell) %>;
  </script>
    <script src="/script.js"></script>
  </body>
</html>

