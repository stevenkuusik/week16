<!DOCTYPE html>
<html lang="et">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CV – <%= cv.steven %> <%= cv.kuusik %></title>
  <link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@400;600;700&display=swap" rel="stylesheet">

  <style>
    body {
      font-family: 'Open Sans', sans-serif;
      background: #ffffff;
      margin: 0;
      padding: 40px;
      display: flex;
      justify-content: center;
    }

    .cv-container {
      background: #ffffff;
      width: 1200px;
      border-radius: 6px;
      padding: 30px;
      box-sizing: border-box;
      box-shadow: 0 4px 20px rgba(0,0,0,0.15);
      border: 2px solid #3da0ff;
    }

    .top {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      border-bottom: 1px solid #ddd;
      padding-bottom: 20px;
      margin-bottom: 20px;
    }

    .profile {
      display: flex;
      align-items: center;
      gap: 15px;
    }
    .profile img {
      width: 70px;
      height: 70px;
    }
    .profile-info h2 {
      margin: 0;
      font-size: 22px;
      font-weight: 700;
      color: #222;
    }
    .profile-info {
      font-size: 14px;
      color: #444;
    }
    .contact-row {
      margin-top: 6px;
    }
    .contact-row span {
      margin-right: 15px;
    }

    .licenses {
      text-align: right;
      font-size: 14px;
      color: #444;
    }
    .licenses h3 {
      margin: 0 0 5px 0;
      font-size: 16px;
      font-weight: 600;
      color: #333;
    }

    .stars {
      margin-top: 10px;
      font-size: 22px;
      color: #ccc;
    }
    .stars .filled {
      color: gold;
    }

    .content {
      display: grid;
      grid-template-columns: 1fr 2fr;
      gap: 20px;
    }

    .left, .right {
      display: flex;
      flex-direction: column;
      gap: 20px;
    }

    .section {
      background: #f0f0f0;
      border-radius: 6px;
      padding: 15px;
      box-shadow: 0 2px 6px rgba(0,0,0,0.05);
    }
    .section h3 {
      margin-top: 0;
      font-size: 16px;
      font-weight: 600;
      border-bottom: 1px solid #ddd;
      padding-bottom: 6px;
      margin-bottom: 10px;
      color: #333;
    }
    .section p {
      margin: 6px 0;
      font-size: 14px;
      line-height: 1.5;
    }

    .chart-img {
      width: 100%;
      border-radius: 6px;
      cursor: pointer;
    }

    .social {
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 30px;
      margin-top: 50px;
    }
    .social img {
      height: 45px;
      cursor: pointer;
      transition: transform 0.2s ease;
    }
    .social img:hover {
      transform: scale(1.05);
    }

    @media (max-width: 480px) {
      body {
        padding: 10px;
      }

      .cv-container {
        width: 100%; 
        padding: 15px;
      }

      .top {
        flex-direction: column; 
        align-items: flex-start;
      }

      .content {
        grid-template-columns: 1fr;
      }

      .profile img {
        width: 60px;
        height: 60px;
      }

      .profile-info h2 {
        font-size: 18px;
      }

      .section {
        padding: 10px;
      }

      .section h3 {
        font-size: 14px;
      }

      .section p {
        font-size: 13px;
      }

      .social {
        justify-content: center;
        gap: 20px;
        margin-top: 20px;
      }

      .social img {
        height: 35px;
      }
    }
  </style>
</head>
<body>
  <div class="cv-container">

    <div class="top">
      <div class="profile">
        <img src="/<%= cv.profiil %>" alt="Profiil" />
        <div class="profile-info">
          <h2><%= cv.nimi %> <%= cv.perekonnanimi %></h2>
          <div class="contact-row"><%= cv.asukoht %></div>
          <div class="contact-row">
            <span>📞 <%= cv.kontakt.telefon %></span>
            <span>✉️ <%= cv.kontakt.email %></span>
          </div>
          <div class="contact-row">📍 <%= cv.kontakt.aadress %></div>
          <div class="stars">
            <span class="filled">★</span>
            <span class="filled">★</span>
            <span class="filled">★</span>
            <span class="filled">★</span>
            <span class="filled">★</span>
          </div>
        </div>
      </div>
      <div class="licenses">
        <h3>Juhiload</h3>
        <%= cv.juhiload.join(", ") %>
      </div>
    </div>

    <div class="content">
      <div class="left">
        <div class="section">
          <h3>Keeleoskus</h3>
          <% cv.keeled.forEach(k => { %>
            <p><%= k.nimi %> — <%= k.tase %></p>
          <% }) %>
        </div>

        <div class="section">
          <h3>Arvutioskused</h3>
          <% cv.oskused.forEach(o => { %>
            <p><%= o.nimi %> — <%= o.tase %></p>
          <% }) %>
        </div>

        <div class="section">
          <h3>Huvialad</h3>
          <% cv.huvialad.forEach(h => { %>
            <p><%= h %></p>
          <% }) %>
        </div>

        <img id="carousel-img" class="chart-img" src="/<%= cv.karussell[0] %>" alt="Pilt">
        <div class="social"></div>
      </div>

      <div class="margin-right">
        <div class="section">
          <h3>Hariduskäik</h3>
          <% cv.haridus.forEach(h => { %>
            <p><strong><%= h.kool %></strong> — <%= h.aasta %></p>
          <% }) %>
        </div>

        <div class="section">
          <h3>Töökogemus</h3>
          <% cv.tootamine.forEach(t => { %>
            <p><strong><%= t.firma %></strong> — <%= t.amet %> (<%= t.aasta %>)<br>
            <%= t.kirjeldus %></p>
          <% }) %>
        </div>

        <div class="right">
          <div class="section">
            <h3>Täiendav info</h3>
            <p><b>Tugevad küljed:</b> <%= cv.tugevad.join(", ") %></p>
            <p><b>Nõrgad küljed:</b> <%= cv.norgad.join(", ") %></p>
            <p><b>Ajateenistus:</b> <%= cv.ajateenistus %></p>
            <p><b>Praktikale võimalik astuda:</b> <%= cv.praktika %></p>
          </div>
          <div class="social">
             <a href="https://instagram.com" target="_blank">
                <img src="/<%= cv.social.instagram %>" alt="Instagram">
             </a>
                <a href="https://facebook.com" target="_blank">
             <img src="/<%= cv.social.facebook %>" alt="Facebook">
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>

<script>
  window.carouselImages = <%- JSON.stringify(cv.karussell) %>;
</script>
<script src="/script.js"></script>
